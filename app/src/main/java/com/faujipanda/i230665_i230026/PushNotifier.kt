package com.faujipanda.i230665_i230026

object PushNotifier {
    fun sendNotification(context: android.content.Context, title: String, body: String, targetUserId: Int) {
        // Push notifications removed - implement with API if needed
    }
}
